spring:
  application:
    name: user
  profiles:
    active: development
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configserver
      # label 对应 Git branch
      label: user
      # name 对应文件名
      name: user
      username: cloudapp
      password: FD3A86C4-3822-4551-9903-3E0B55B1EE65

eureka:
  client:
    serviceUrl:
      # defaultZone: http://discovery:8761/eureka/
      defaultZone: http://${discovery.host:localhost}:8761/eureka/
  instance:
    preferIpAddress: true
    hostname: user

encrypt:
  failOnError: false

